<?php /** @var stdClass $view */ ?>
<?php require('template/header.phtml') ?>

    <main class="flex-grow-1">
        <?php
        // Check if pet data failed to load (e.g., due to permission error or invalid ID)
        if (isset($view->formMessage) && $view->pet == null):
            ?>
            <p><?php echo $view->formMessage?></p>
            <a href="mypets.php">Return to my listings</a>
        <?php else:?>
            <?php
            // Display status messages after a form submission (Success/No changes made)
            if (isset($view->formMessage) && $view->formMessage != ''):
                ?>
                <div class="alert alert-success">
                    <?php echo $view->formMessage; ?>
                </div>
            <?php endif; ?>
            <div class="row">
                <div class="col-lg-8 col-md-10 mx-auto">
                    <div class="card bg-light mb-4">
                        <div class="card-body">
                            <form action="editpet.php" method="POST" enctype="multipart/form-data">
                                <!-- Hidden input to pass the unique Pet ID back to the controller -->
                                <input type="hidden" name="petID" value="<?php echo $view->pet->getID()?>">

                                <div class="mb-3">
                                    <!-- Input fields are pre-filled with the current pet's data -->
                                    <label  class="form-label" for="name">Pet name</label>
                                    <input class="form-control" type="text" name="petName" id="name" value="<?php echo $view->pet->getName() ?>" required>

                                    <label class="form-label" for="species">Species</label>
                                    <input class="form-control" type="text" name="species" id="species" value="<?php echo $view->pet->getSpecies() ?>" required>

                                    <label class="form-label" for="breed">Breed</label>
                                    <input class="form-control" type="text" name="breed" id="breed" value="<?php echo $view->pet->getBreed() ?>">

                                    <label class="form-label" for="color">Color</label>
                                    <input  class="form-control" type="text" name="color" id="color" value="<?php echo $view->pet->getColor() ?>">

                                    <label class="form-label" for="description">Description</label>
                                    <input class="form-control" type="text" name="description" id="description" value="<?php echo $view->pet->getDescription() ?>">

                                    <label class="form-label">Status</label>
                                    <div class="form-control d-flex align-items-center gap-3 py-2">
                                        <div class="form-check form-check-inline">
                                            <!-- Radio button checked if pet status is 'lost' -->
                                            <input class="form-check-input" type="radio" name="status" id="status_lost" value="lost" <?php if ($view->pet->getStatus() == 'lost') echo 'checked'; ?>>
                                            <label class="form-check-label" for="status_lost">Lost 😔</label>
                                        </div>
                                        <div class="form-check form-check-inline">
                                            <!-- Radio button checked if pet status is 'found' -->
                                            <input class="form-check-input" type="radio" name="status" id="status_found" value="found" <?php if ($view->pet->getStatus() == 'found') echo 'checked'; ?>>
                                            <label class="form-check-label" for="status_found">Found 🥳</label>
                                        </div>
                                    </div>

                                    <div class="form-group">
                                        <input type="submit"  class="btn btn-outline-primary mt-3" name="submit" value="Update Pet">
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        <?php endif; ?>
    </main>

<?php require('template/footer.phtml') ?>