<?php /** @var stdClass $view */ ?>
<!doctype html>
<html lang="en">
<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="description" content="">
    <meta name="author" content="">

    <!-- Link required CSS files -->
    <link href="Views/css/bootstrap.css" rel="stylesheet">
    <link href="Views/css/bootstrap-theme.css" rel="stylesheet">
    <link href="Views/css/my-style.css" rel="stylesheet">

    <!-- Set page title dynamically from the controller -->
    <title>Client Server Systems - <?php echo $view->pageTitle; ?></title>
</head>

<body class="d-flex flex-column min-vh-100">


<div class="container-fluid p-3">

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="#">
            <img src="/images/logo.jpg" alt="" width="30" height="24" class="d-inline-block align-text-top">
            PetWatch
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="navbar-brand" href="index.php">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="listpets.php">View All Pets</a>
                </li>
                <!-- Check if the user is logged in to show user-specific links -->
                <?php if (isset($_SESSION['username'])): ?>
                    <!-- Check if user has the 'admin' role -->
                    <?php if (isset($_SESSION['role']) && $_SESSION['role'] == 'admin'): ?>
                        <!-- Admin links (Manage Listings, Create/View Sightings) -->
                        <li class="nav-item">
                            <a class="nav-link" href="mypets.php">Manage Listings</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="addsighting.php">Create Sighting</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="mysightings.php">View Sightings</a>
                        </li>
                    <?php else: ?>
                        <!-- Non-admin links (Only Create/View Sightings) -->
                        <li class="nav-item">
                            <a class="nav-link" href="addsighting.php">Create Sighting</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="mysightings.php">View Sightings</a>
                        </li>
                    <?php endif; ?>
                <?php endif; ?>

            </ul>
            <ul class="navbar-nav">
                <?php
                // Display user details and logout link if logged in
                if (isset($_SESSION['username'])):
                    ?>
                    <li class="nav-item">
                        <span class="navbar-text">
                            Welcome, <?php echo htmlspecialchars($_SESSION['username']);?>!
                        </span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="logout.php">Logout</a>
                    </li>
                <?php
                // Display login link if logged out
                else:
                    ?>
                    <li class="nav-item">
                        <span class="navbar-text">No user logged in.</span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="login.php">Login</a>
                    </li>
                <?php endif; ?>
            </ul>
        </div>
    </nav>

    <div class="row">
        <div class="col-12">
            <h1><?php echo $view->pageTitle ?> </h1>
            <hr class="mb-3">
        </div>
    </div>